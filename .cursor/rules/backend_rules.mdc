---
description: 
globs: 
alwaysApply: true
---
# 後端開發規則 (Backend Rules)

## PHP版本要求
- 使用PHP 8.0或更高版本
- 啟用必要的擴展：mysqli, json, curl, mbstring

## 目錄結構
```
backend/
├── api/
│   ├── auth.php          # 簡單用戶驗證
│   ├── code.php          # 代碼操作API
│   ├── room.php          # 房間管理API
│   ├── history.php       # 歷史紀錄API
│   ├── ai.php            # AI助教API
│   └── monitor.php       # 教師監控API
├── classes/
│   ├── Database.php      # 資料庫連接類
│   ├── Room.php          # 房間管理類
│   ├── CodeManager.php   # 代碼管理類
│   ├── ConflictDetector.php # 衝突檢測類
│   ├── AIAssistant.php   # AI助教類
│   └── Logger.php        # 日誌記錄類
├── config/
│   ├── database.php      # 資料庫配置
│   ├── openai.php        # OpenAI配置
│   └── app.php           # 應用配置
└── utils/
    ├── response.php      # API響應工具
    ├── validation.php    # 數據驗證工具
    └── security.php      # 安全工具
```

## API設計規範
1. 所有API返回JSON格式
2. 統一的響應結構：
   ```json
   {
     "success": true/false,
     "message": "操作結果訊息",
     "data": {},
     "timestamp": "2024-01-01T00:00:00Z"
   }
   ```
3. 錯誤處理必須記錄到日誌
4. 所有輸入數據必須驗證和清理

## 資料庫設計
- 使用MySQL 8.0+
- 所有表名使用小寫和下劃線
- 主鍵統一使用`id`
- 時間戳字段：`created_at`, `updated_at`

## 安全規範
1. 所有用戶輸入必須過濾
2. SQL查詢使用預處理語句
3. API密鑰安全存儲
4. 跨域請求適當配置

## 錯誤處理
1. 所有異常必須捕獲並記錄
2. 用戶友好的錯誤訊息
3. 詳細的開發者日誌
4. 錯誤代碼標準化

## 性能要求
1. API響應時間 < 500ms
2. 資料庫查詢優化
3. 適當的緩存策略
4. 連接池管理

