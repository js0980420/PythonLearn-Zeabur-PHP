---
description: 
globs: 
alwaysApply: true
---
# WebSocket規則 (WebSocket Rules)

## Ratchet WebSocket實現

### 安裝要求
```bash
composer require ratchet/pawl
composer require ratchet/rfc6455
```

### 服務器結構
```
websocket/
├── server.php           # WebSocket服務器主文件
├── handlers/
│   ├── ConnectionHandler.php  # 連接處理器
│   ├── RoomHandler.php        # 房間處理器
│   ├── CodeSyncHandler.php    # 代碼同步處理器
│   └── ConflictHandler.php    # 衝突處理器
├── models/
│   ├── Room.php         # 房間模型
│   ├── User.php         # 用戶模型
│   └── CodeState.php    # 代碼狀態模型
└── config/
    └── websocket.php    # WebSocket配置
```

## 連接管理
1. **用戶識別**：
   - 使用簡單的session ID或隨機生成的用戶ID
   - 支援無痕瀏覽器連接
   - 連接時自動分配臨時用戶名

2. **房間管理**：
   - 房間ID格式：`room_[timestamp]_[random]`
   - 最大用戶數：50人/房間
   - 自動清理空房間

3. **連接狀態**：
   - 心跳檢測：每30秒
   - 斷線重連：自動重試3次
   - 連接超時：5分鐘無活動自動斷開

## 消息協議
### 消息格式
```json
{
  "type": "message_type",
  "room_id": "room_identifier",
  "user_id": "user_identifier",
  "timestamp": "2024-01-01T00:00:00Z",
  "data": {}
}
```

### 消息類型
1. **join_room** - 加入房間
2. **leave_room** - 離開房間
3. **code_change** - 代碼變更
4. **cursor_position** - 游標位置
5. **conflict_detected** - 衝突檢測
6. **conflict_resolution** - 衝突解決
7. **chat_message** - 聊天訊息
8. **user_list** - 用戶列表更新

## 代碼同步機制
1. **實時同步**：
   - 每次按鍵都發送變更
   - 使用操作轉換(OT)算法
   - 保持代碼一致性

2. **衝突檢測**：
   - 檢測同一行的並發編輯
   - 檢測重疊區域的修改
   - 實時標記衝突區域

3. **版本控制**：
   - 每次變更記錄版本號
   - 支援回滾到之前版本
   - 變更歷史記錄

## 性能優化
1. **消息批處理**：
   - 100ms內的變更合併發送
   - 減少網絡請求頻率

2. **內存管理**：
   - 定期清理斷開的連接
   - 限制房間內存使用

3. **負載均衡**：
   - 支援多個WebSocket服務器
   - 房間分片策略

## 錯誤處理
1. **連接錯誤**：
   - 自動重連機制
   - 錯誤狀態通知

2. **消息錯誤**：
   - 無效消息過濾
   - 錯誤消息記錄

3. **房間錯誤**：
   - 房間不存在處理
   - 權限驗證失敗

## 安全考慮
1. **輸入驗證**：
   - 所有消息內容驗證
   - 防止XSS攻擊

2. **頻率限制**：
   - 每用戶每秒最多10條消息
   - 防止垃圾消息

3. **房間權限**：
   - 簡單的房間密碼保護
   - 教師管理權限

