---
description: 
globs: 
alwaysApply: false
---
# 📺 教師監控後台規則文檔

## 🎯 監控系統設計理念

### 核心概念
教師監控後台應該像「監視器直播系統」一樣，提供即時、全螢幕的學生代碼編輯監控體驗。

### 設計原則
1. **即時性** - 學生每次按鍵都要即時顯示
2. **全螢幕** - 代碼區域佔據半個螢幕或更大
3. **直觀性** - 一眼就能看到所有學生的編程狀態
4. **無干擾** - 不需要點擊「詳細檢視」等額外操作

## 📐 界面佈局規範

### 主要區域劃分
```
┌─────────────────────────────────────────────────────────────┐
│ 🎛️ 控制面板 (頂部 10%)                                        │
├─────────────────────────────────────────────────────────────┤
│ 📊 房間概覽 (左側 30%)     │ 📺 代碼監控區 (右側 70%)        │
│                           │                                │
│ • 房間列表                │ • 學生代碼即時顯示              │
│ • 學生狀態                │ • CodeMirror 編輯器             │
│ • 活動統計                │ • 語法高亮                     │
│                           │ • 即時更新                     │
│                           │                                │
├─────────────────────────────────────────────────────────────┤
│ 📝 活動日誌 (底部 20%)                                        │
└─────────────────────────────────────────────────────────────┘
```

### 代碼監控區詳細規範

#### 尺寸要求
- **最小寬度**: 50vw (視窗寬度的50%)
- **最小高度**: 60vh (視窗高度的60%)
- **最大化模式**: 可展開至 80vw × 80vh

#### CodeMirror 配置
```javascript
{
  mode: "python",
  theme: "monokai",
  lineNumbers: true,
  readOnly: true,
  lineWrapping: true,
  fontSize: "14px",
  autoRefresh: true,
  viewportMargin: Infinity
}
```

## 🔄 即時更新機制

### WebSocket 事件監聽
```javascript
// 監聽學生代碼變更
socket.on('student_code_change', (data) => {
  updateStudentCodeView(data.studentId, data.code);
});

// 監聽學生活動狀態
socket.on('student_activity', (data) => {
  updateStudentStatus(data.studentId, data.activity);
});
```

### 更新頻率
- **代碼變更**: 即時更新 (每次按鍵)
- **狀態刷新**: 每 500ms
- **連線檢查**: 每 5 秒

## 👥 多學生監控模式

### 單學生模式 (預設)
- 顯示選中學生的完整代碼
- 代碼區域佔據 70% 螢幕寬度
- 支援語法高亮和行號顯示

### 多學生網格模式
```
┌─────────────┬─────────────┬─────────────┐
│ 學生A代碼   │ 學生B代碼   │ 學生C代碼   │
│ (縮小版)    │ (縮小版)    │ (縮小版)    │
├─────────────┼─────────────┼─────────────┤
│ 學生D代碼   │ 學生E代碼   │ 學生F代碼   │
│ (縮小版)    │ (縮小版)    │ (縮小版)    │
└─────────────┴─────────────┴─────────────┘
```

### 分割螢幕模式
```
┌─────────────────────┬─────────────────────┐
│ 學生A代碼 (主要)     │ 學生B代碼 (次要)     │
│ 50% 螢幕寬度        │ 50% 螢幕寬度        │
│                    │                    │
│ 完整語法高亮        │ 完整語法高亮        │
└─────────────────────┴─────────────────────┘
```

## 🎨 視覺設計規範

### 色彩系統
```css
:root {
  --monitor-bg: #1a1a1a;           /* 監控背景 */
  --code-bg: #2d3748;              /* 代碼背景 */
  --active-student: #48bb78;       /* 活躍學生 */
  --inactive-student: #718096;     /* 非活躍學生 */
  --error-highlight: #f56565;      /* 錯誤高亮 */
  --success-highlight: #68d391;    /* 成功高亮 */
}
```

### 字體規範
- **代碼字體**: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace
- **代碼大小**: 14px (可調整 12px-18px)
- **界面字體**: 'Microsoft YaHei', sans-serif
- **界面大小**: 16px

### 動畫效果
```css
/* 代碼更新動畫 */
.code-update {
  animation: codeFlash 0.3s ease-in-out;
}

@keyframes codeFlash {
  0% { background-color: rgba(72, 187, 120, 0.3); }
  100% { background-color: transparent; }
}

/* 學生狀態指示器 */
.student-indicator {
  animation: pulse 2s infinite;
}
```

## 📊 監控數據顯示

### 學生狀態指示器
```html
<div class="student-status">
  <span class="status-dot active"></span>
  <span class="student-name">張同學</span>
  <span class="last-activity">2秒前</span>
  <span class="code-lines">45行</span>
</div>
```

### 代碼統計信息
- **總行數**: 即時統計
- **最後修改時間**: 精確到秒
- **編輯頻率**: 每分鐘按鍵次數
- **語法錯誤**: 即時檢測並標記

## 🔧 功能按鈕配置

### 監控控制按鈕
```html
<div class="monitor-controls">
  <button class="btn-fullscreen">🔍 全螢幕</button>
  <button class="btn-grid-view">📱 網格檢視</button>
  <button class="btn-split-view">⚡ 分割檢視</button>
  <button class="btn-auto-follow">👁️ 自動跟隨</button>
  <button class="btn-pause">⏸️ 暫停更新</button>
</div>
```

### 快速操作
- **雙擊學生名稱**: 切換到該學生的全螢幕檢視
- **Ctrl + 滾輪**: 調整代碼字體大小
- **空白鍵**: 暫停/恢復即時更新
- **F11**: 進入全螢幕監控模式

## 📱 響應式設計

### 桌面端 (≥1200px)
- 代碼區域: 70% 寬度
- 支援多學生並排顯示
- 完整功能按鈕

### 平板端 (768px-1199px)
- 代碼區域: 60% 寬度
- 簡化控制按鈕
- 支援觸控操作

### 手機端 (<768px)
- 代碼區域: 100% 寬度
- 垂直堆疊佈局
- 手勢控制

## 🚨 異常狀態處理

### 連線中斷
```html
<div class="connection-lost">
  ⚠️ 與學生連線中斷
  <button class="btn-reconnect">重新連線</button>
</div>
```

### 代碼錯誤
```html
<div class="code-error">
  ❌ 語法錯誤 (第15行)
  <span class="error-detail">SyntaxError: invalid syntax</span>
</div>
```

### 學生離線
```html
<div class="student-offline">
  😴 學生已離線
  <span class="offline-time">5分鐘前</span>
</div>
```

## 🎯 效能優化規範

### 代碼更新優化
- 使用 `requestAnimationFrame` 控制更新頻率
- 實施代碼差異比較，只更新變更部分
- 限制同時監控的學生數量 (最多8人)

### 記憶體管理
- 定期清理舊的代碼歷史記錄
- 使用虛擬滾動處理長代碼
- 實施代碼快取機制

### 網路優化
- 壓縮 WebSocket 傳輸數據
- 使用增量更新而非全量更新
- 實施斷線重連機制

## 📋 實施檢查清單

### 基礎功能 ✅
- [ ] CodeMirror 整合
- [ ] WebSocket 即時通訊
- [ ] 學生狀態監控
- [ ] 代碼語法高亮

### 進階功能 🚀
- [ ] 多學生網格檢視
- [ ] 全螢幕監控模式
- [ ] 代碼差異比較
- [ ] 自動跟隨活躍學生

### 優化功能 ⚡
- [ ] 效能監控
- [ ] 錯誤處理
- [ ] 響應式設計
- [ ] 無障礙支援

## 🔮 未來擴展功能

### AI 輔助監控
- 自動檢測學生編程困難
- 智能推薦教學建議
- 代碼品質評估

### 協作功能
- 教師即時代碼註解
- 學生求助系統
- 群組討論整合

### 數據分析
- 學習進度追蹤
- 編程習慣分析
- 效能報告生成

---

**📝 文檔版本**: v1.0  
**📅 最後更新**: 2025-06-02  


**👨‍💻 維護者**: Python協作教學平台團隊 